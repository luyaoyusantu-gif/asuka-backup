# 2026-03-19 记忆日志

## 重要配置变更

### 添加 MiniMax M2.7 模型
- **时间**：2026-03-19 12:39
- **操作**：通过 `gateway config.patch` 添加 MiniMax M2.7 到 openclaw.json
- **位置**：`models.providers.minimax-cn.models` 和 `models.providers.minimax-portal.models`
- **别名**：在 `agents.defaults.models` 中添加了 `minimax-cn/MiniMax-M2.7` 和 `minimax-portal/MiniMax-M2.7`
- **配置**：contextWindow: 200000, maxTokens: 8192, reasoning: true, 成本沿用 M2.5 (input: 0.3, output: 1.2)
- **备份**：已创建 `openclaw.json.backup.20260319_103705`
- **网关**：已自动重启生效

## 模型切换记录

| 时间 | 操作 | 模型 |
|------|------|------|
| 上午 | 切换到 DeepSeek Chat (`custom-custom91/deepseek-chat`) | 用户要求 |
| 上午 | 切换回 DeepSeek R1 (`custom-custom91/deepseek-reasoner`) | 用户澄清 R1 = reasoner |
| 12:44 | 切换到 MiniMax M2.7 (`minimax-cn/MiniMax-M2.7`) | 用户要求测试新模型 |
| 12:47 | **设为主要模型**，DeepSeek R1 降为备用 | 用户决策 |

## 主要模型更新

- **时间**：2026-03-19 12:47
- **操作**：将 `agents.defaults.model.primary` 从 `custom-custom91/deepseek-reasoner` 改为 `minimax-cn/MiniMax-M2.7`
- **备用模型**：`custom-custom91/deepseek-reasoner`（DeepSeek R1）
- **网关**：已自动重启生效
- **影响**：之后所有新对话默认使用 MiniMax M2.7，API 失败时自动回退到 DeepSeek R1

## Ollama 排查（未解决）

- **现象**：Ollama 进程存在（PID 40316），但端口 11434 未监听
- **状态**：命令行 `ollama` 不在 PATH 中，只能通过 `$env:LOCALAPPDATA\Programs\Ollama\ollama.exe` 调用
- **错误**：调用时提示 "existing instance found, not focusing due to startHidden"
- **结论**：服务启动异常，但进程存活
- **后续**：用户未授权重启，建议手动重启 Ollama 应用

## 定时清理提醒问题

- **现象**：收到多次 "openclaw sessions cleanup" 定时提醒（每小时一次：04:10, 06:10, 08:10, 10:10）
- **排查**：
  - OpenClaw Cron 列表中无此任务
  - Windows 任务计划中无此任务
  - 飞书日历需要授权才能搜索
- **结论**：来源不明，可能是之前某次会话创建的或外部工具
- **用户请求**：希望改为 12 小时一次，但未找到源头
- **状态**：待处理

## 其他

- **诺玛 MiniMax 调用问题**：用户反映诺玛无法调用 MiniMax API。我这边使用相同密钥（minimax-cn:default）能正常切换和调用，说明密钥有效，问题可能在于诺玛的 baseUrl、api 类型或模型 ID 配置不一致。
- **Sessions 清理**：已执行多次，条目保持 8 个，不影响记忆文件。

## 小米 MiMo-V2-Pro 调研（2026-03-19 12:51）

### 发布信息
- **发布时间**：2026-03-19（当天）
- **型号**：MiMo-V2-Pro（旗舰基座大模型）
- **其他型号**：MiMo-V2-Omni（全模态Agent）、MiMo-V2-TTS（语音合成）

### 核心参数
- **上下文窗口**：100万 tokens（1M）
- **总参数量**：1万亿（活跃420亿）
- **全球排名**：第8名，中国第2，超越 xAI Grok
- **API定价**（256K内）：
  - 输入：$1/1M tokens
  - 输出：$3/1M tokens
  - 缓存读取：$0.20/1M tokens
  - 缓存写入：免费
- **LiteLLM调用格式**：`xiaomi_mimo/mimo-v2-pro`

### vs MiniMax-M2.7 对比
| 项目 | MiMo-V2-Pro | MiniMax-M2.7 |
|------|-------------|--------------|
| 上下文 | 1M | 200K |
| 价格 | $1/1M | ¥0.3/1M（约$0.04) |
| 排名 | 全球第8 | 未上榜 |
| 多模态 | ✅ | 文本为主 |

### 用户意向
- 用户想了解 MiMo vs MiniMax 的优劣势
- 结论：MiMo 性能强、价格贵；MiniMax 便宜稳定
- 建议：保持 MiniMax 主要，MiMo 作为备用（有免费试用期）
- **待操作**：用户还未决定是否添加 MiMo 到配置

## 与诺玛的协作（12:55）

### 诺玛请求配置信息
- 诺玛通过卢垚宇发来请求，需要明日香的 MiniMax 配置来排查她无法调用 MiniMax API 的问题
- 分享内容：
  - Base URL: `https://api.minimaxi.com/anthropic`
  - API 类型: `anthropic-messages`
  - 认证方式: `authHeader: true`（使用 Authorization Header）
  - 模型 ID: `MiniMax-M2.7` 和 `MiniMax-M2.5`
- **注意**：未分享 API 密钥，让诺玛用自己的密钥

### 可能的问题排查点
1. Base URL 是否一致（注意是 `anthropic` 不是 `v1`）
2. API 类型是否选 `anthropic-messages`（不是 `openai-completions`）
3. 认证方式：需带 `Authorization: Bearer <API_KEY>` 请求头
